---
interface Props {
  title: string;
  description: string;
  url: string;
  image: string | null;
}

const { title, description, url, image } = Astro.props;

// Usamos una imagen de relleno si la del CMS falla o no existe
const imageUrl = image 
  ? `https://cms.romahomestore.com/assets/${image}` 
  : 'https://placehold.co/600x400/222/fff?text=Proyecto'; 
---

<a 
  href={url} 
  target="_blank" 
  class="block p-4 border border-gray-700 rounded-xl bg-gray-900/50 hover:border-blue-500 transition-all group"
>
  <div class="aspect-video w-full overflow-hidden rounded-lg mb-4 bg-gray-800">
    <img 
      src={imageUrl} 
      alt={title} 
      class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
      loading="lazy"
    />
  </div>
  
  <h3 class="text-xl font-bold text-white mb-2">{title}</h3>
  <p class="text-gray-400 text-sm line-clamp-2">{description}</p>
</a>